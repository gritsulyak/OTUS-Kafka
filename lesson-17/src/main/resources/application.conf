kafka {
    topic = "lesson-17-test"
    consumer: ${pekko.kafka.consumer} {
      kafka-clients {
        bootstrap.servers = "localhost:19092"
      }
    }

    producer: ${pekko.kafka.producer} {
        kafka-clients {
            bootstrap.servers = "localhost:19092"
        }
    }
}
